/**
 * Legacy Vite React entrypoint preserved for reference.
 * The Next.js application uses src/pages routes instead.
 */

/*
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
*/

export default null;
